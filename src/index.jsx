import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';

const rootElement = document.getElementById('sceneContainer');
ReactDOM.render(<App />, rootElement);
